export default (theme) => ({
  clusterMarker: {
    color: '#fff',
    background: '#dc230e',
    borderRadius: '50%',
    padding: '20px',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontWeight: 'bolder'
  }
})
